{
  "name": "aditya-os",
  "version": "1.0.0",
  "description": "ADITYA Sentient Cognitive Operating System Workspace",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && ..\\.venv\\Scripts\\python.exe main.py --no-ui",
    "dev:electron": "cd client && npm run electron:dev",
    "build:backend": "cd backend && ..\\.venv\\Scripts\\pyinstaller.exe --onefile --noconsole --name AdityaCore --distpath dist --workpath build main.py",
    "build:electron": "cd client && npm run electron:dist"
  }
}
