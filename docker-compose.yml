version: '3.8'

services:
  aditya-os:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7865:7865"
    volumes:
      - ./CognitiveCore/data:/app/CognitiveCore/data
    environment:
      - PYTHONUNBUFFERED=1
      - ENV=development
    restart: unless-stopped
