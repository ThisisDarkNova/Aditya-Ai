// Particle controller logic isolated for reuse
export class ParticleEngine {
    constructor(canvas) {
        this.canvas = canvas;
        this.ctx = canvas.getContext('2d');
        this.particles = [];
    }
    // Isolated core animation logic
}
